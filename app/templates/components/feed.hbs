<div class="feed-wrapper">
	{{#each @tweets as |tweet|}}

		<Tweet 
			@data={{tweet}}
            @onDelete={{this.onDelete}}
		/>

	{{else}}
		
		{{!-- TODO: empty state --}}
		<p>No hay tweets</p>

	{{/each}}
</div>